# Copy file này thành .env.local rồi bỏ comment khi cần.

# Đọc model từ public/models/ thay vì tải từ huggingface.co.
# Chạy `npm run fetch-models` trước để có model trong đó.
# Dùng cho buổi thực hành ở phòng máy có mạng chậm hoặc không có Internet.
# VITE_LOCAL_MODELS=true
